/**
 * Configuration file for Google Maps Automation
 * Define constants and settings here
 */

// The package name of Google Maps app
export const APP_PACKAGE_NAME = 'com.google.android.apps.maps';

// Maximum steps (iterations) per stage before failing with OUT_OF_STEPS
export const DEFAULT_MAX_STEPS_PER_STAGE = 48;
