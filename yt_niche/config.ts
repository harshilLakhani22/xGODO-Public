/**
 * Configuration file for the YouTube Niche Spy automation
 */

// Firefox browser package name
export const FIREFOX_PACKAGE = 'org.mozilla.firefox';

// YouTube URL to navigate to
export const YOUTUBE_URL = 'https://m.youtube.com/';

// Maximum steps (iterations) per stage before failing with OUT_OF_STEPS
export const DEFAULT_MAX_STEPS_PER_STAGE = 48;
